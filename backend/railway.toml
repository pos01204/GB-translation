# Railway 배포 설정

[build]
builder = "dockerfile"
dockerfilePath = "./Dockerfile"

[deploy]
# startCommand는 Dockerfile CMD 사용
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
